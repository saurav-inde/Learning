#include <stdio.h>

/*-----------------------------------------------------------------------------------------------------
-------------------------------------main section----------------------------------------------------*/
int main()
{

    float num1, num2;
    printf("Enter numbers: ");
    scanf("%f%f", &num1, &num2);
    if (num1 > num2)
    {
        printf("%.2f is greater than %.2f", num1, num2);
    }

    else if (num1 < num2)
    {
        printf("%.2f is greater than %.2f", num2, num1);
    }

    else
    {
        printf("%.2f is equal to %.2f", num2, num1);
    }
    return 0;
}

/*-----------------------------------------------------------------------------------------------------
--------------------------------subprogram section---------------------------------------------------*/